const panels = require("../schemas/panels");

module.exports = {
    name: "guildMemberUpdate",
    async execute(oldMember, newMember) {
        // const guildPanels = await panels.find({ guildid: member.guild.id });
        // if (guildPanels.length == 0) return;

        // Quando viene aggiornato lo stato di un utente si confrontano i ruoli del pannello rispetto ai suoi ruoli

        console.log(oldMember._roles.includes('992150476973944853'), newMember._roles.includes('992150476973944853'));
    }
}